import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customer',
};

export default function customer () {
    return (
        <div>
            <h1>Customer</h1>
        </div>
    )
}